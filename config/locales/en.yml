en:
  activerecord:
    errors:
      models:
        product:
          attributes:
            image:
              wrong_format: "must be a valid image format"
              too_big: "Img size is too big"
